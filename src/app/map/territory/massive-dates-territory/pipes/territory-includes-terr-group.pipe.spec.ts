import { TerritoryIncludesTerrGroupPipe } from './territory-includes-terr-group.pipe';

describe('TerritoryIncludesTerrGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new TerritoryIncludesTerrGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
