import { SheetTitlePipe } from './sheet-title.pipe';

describe('SheetTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SheetTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
